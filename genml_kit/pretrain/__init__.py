"""Self-supervised pre-training losses and augmentations.

v4.2: the pre-training *methods* moved to ``genml_kit.methods`` and the
``genml-kit-pretrain`` binary was folded into the unified
``genml-kit-train`` CLI (plans/GENERIC_PIPELINE.md s 5.2).  This package
keeps the loss and augmentation libraries that the methods compose.
"""
